name: gamez
description: A small 2D game engine to easily create games in Flutter by providing different methods for rendering, gesture detection, etc.
version: 0.0.3
homepage: https://github.com/adilayman/gamez

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.4
  provider: ^6.0.1

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter: null
